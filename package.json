{
  "name": "pc-swiper",
  "version": "1.0.0",
  "description": "preact swiper flow control by rxjs",
  "main": "index.js",
  "scripts": {
    "start": "per-env",
    "start:production": "npm run -s serve",
    "start:development": "npm run -s dev",
    "module": "",
    "build": "preact build --sw true",
    "serve": "preact build --sw true && sirv build --cors --single",
    "dev": "preact watch --sw",
    "lint": "eslint src",
    "test": "jest"
  },
  "keywords": [],
  "author": "DeepKolos <deepkolos@qq.com>",
  "license": "MIT",
  "devDependencies": {
    "enzyme": "^3.11.0",
    "enzyme-adapter-preact-pure": "^2.2.0",
    "eslint": "^6.8.0",
    "eslint-config-preact": "^1.1.1",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^25.2.7",
    "jest-preset-preact": "^1.0.0",
    "per-env": "^1.0.2",
    "preact-cli": "^3.0.0-rc.10",
    "preact-render-spy": "^1.3.0",
    "sirv-cli": "^0.4.5",
    "webpack-alias-sync-plugin": "0.0.1"
  },
  "dependencies": {
    "preact": "^10.3.4",
    "preact-render-to-string": "^5.1.4",
    "preact-router": "^3.2.1",
    "rxjs": "^6.5.5"
  }
}
